;;; Load ELPA
(require 'package)
(package-initialize)
(require 'elpa-config)

;;; Better dired
(require 'dired-x)

;;; cscope - awesome search in c/c++ files
(require 'xcscope)
(require 'cscope-mode-config)

;;; ido magic
(require 'ido-mode-config)
