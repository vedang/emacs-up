(:name flycheck
       :type github
       :pkgname "flycheck/flycheck"
       :description "On-the-fly syntax checking extension"
       ;; Commenting this out because makeinfo is failing on Mac
       ;; :build
       ;; (quote
       ;;  (("makeinfo" "-o" "doc/flycheck.info" "doc/flycheck.texi")))
       ;; :info "./doc"
       :depends
       (dash pkg-info let-alist cl-lib))
